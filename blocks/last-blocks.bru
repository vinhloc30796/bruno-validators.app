meta {
  name: last blocks
  type: http
  seq: 90
  tags: [blocks]
}

get {
  url: {{base_url}}/last-blocks/{{network}}{{filetype}}
}

params:path {
  network: {{network}}
  filetype: {{filetype}}
}

params:query {
  limit: 10
}

headers {
  Token: {{process.env.API_TOKEN}}
  Content-Type: application/json
}
